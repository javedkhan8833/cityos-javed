services:
  fleetops:
    build: .
    container_name: fleetops
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    networks:
      - cityos-network

networks:
  cityos-network:
    external: true